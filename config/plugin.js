'use strict';

module.exports = {
    // static: {
    //   enable: true,
    // }

    decoratorRouter: {
        enable: true,
        package: 'egg-decorator-router'
    },

    sequelize: {
        enable: true,
        package: 'egg-sequelize'
    },

    jwt: {
        enable: true,
        package: 'egg-jwt'
    },

    cache: {
        enable: true,
        package: 'egg-cache'
    }

};
